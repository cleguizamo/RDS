[build]
  command = "npm run build -- --configuration production"
  publish = "dist/frontend-angular/browser"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

