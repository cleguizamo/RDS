export const environment = {
  production: true,
  // Esta URL se configurará durante el build o después del deploy
  // En Railway, usa la URL pública de tu servicio backend
  apiUrl: 'https://your-backend-url.railway.app/api'
};

