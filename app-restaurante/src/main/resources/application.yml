spring:
  application:
    name: app-restaurante
  #Configuracion para la base de datos en railway (MYSQL - posible cambio a AWS)
  datasource:
    url: jdbc:mysql://root:gjTVdzwxbDRoLqxDSnmrOoFvNgimvakK@metro.proxy.rlwy.net:58898/railway
    username: root
    password: gjTVdzwxbDRoLqxDSnmrOoFvNgimvakK
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  jackson:
    deserialization:
      fail-on-unknown-properties: false
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:4200}
    allowed-methods: GET,POST,PUT,DELETE,OPTIONS

#Configuracion para Cloudinary para el almacenamiento de imagenes
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:drp8os7tp}
  api-key: ${CLOUDINARY_API_KEY:446264682988843}
  api-secret: ${CLOUDINARY_API_SECRET:eA6l0j6rJAofqFPBVevQLDrjwMY}

#Configuracion para JWT
jwt:
  secret: ${JWT_SECRET:MiClaveSecretaSuperSeguraParaJWTQueDebeSerMuyLargaYCompleja123456789}
  expiration: ${JWT_EXPIRATION:86400000} # 24 horas en milisegundos

---
spring:
  config:
    activate:
      on-profile: test
  datasource:
    url: jdbc:h2:mem:restaurante;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
